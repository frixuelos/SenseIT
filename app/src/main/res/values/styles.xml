<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#1C4F6B</item>
        <item name="android:textColorSecondary">@color/colorText</item> <!-- Icono toolbar + categorias menu -->
        <item name="android:textColorPrimary">@color/colorText</item> <!-- Texto -->
        <item name="android:itemTextAppearance">@style/optionMenu</item>
    </style>

    <!-- Para las categorias del menu -->
    <style name="MenuText">
        <item name="android:textColor">@color/colorMenuCatText</item>
        <!-- podría añadirse textSize.... -->
    </style>
    <!-- Para el header del menu -->
    <style name="HeaderMenuText">
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">18dp</item>
    </style>

    <style name="optionMenu" parent="@style/Theme.AppCompat.Light">
        <item name="actionMenuTextColor">@color/Black</item>
        <item name="android:actionMenuTextColor">@color/Black</item>
    </style>
</resources>
